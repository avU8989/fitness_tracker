import "../_lifecycle"; // sets up DB hooks
import { describe, it, expect } from "vitest";

describe("fetchWeeklyWorkoutLogs", () => {
    it("works", async () => {
        expect(true).toBe(true);
    });
});